import React from "react";

function Form() {
    return (
        <div>
            <h1>This form is for something </h1>
            <p>Display a line that tells them what the form is for </p>
            {/* form fields will go here
            form content must include:
            text input
            select input for Yes or No
            three more inputs of my choice
            include must validate the content of at least one input  */}
            {/* Create a submit button that sends form data to backend and handle backend response */}
        </div>

    )


}

export default Form;
